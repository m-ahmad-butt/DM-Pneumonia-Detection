# ── ML / Training (run in WSL2 with GPU) ──────────────────────────────────────
tensorflow[and-cuda]    # GPU-accelerated TF; use plain 'tensorflow' on CPU-only
numpy
matplotlib
scikit-image
Pillow
seaborn
tqdm
scikit-learn

# ── Backend API ────────────────────────────────────────────────────────────────
fastapi
uvicorn
python-multipart
pydantic

# ── Shared (image processing) ─────────────────────────────────────────────────
# Use opencv-python-headless everywhere — no GUI dependency, works in WSL2 & server
opencv-python-headless